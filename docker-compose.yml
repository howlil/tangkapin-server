services:
  express-api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "3000:3000"
    
  ml-detection:
    build:
      context: .
      dockerfile: Dockerfile.ml
    depends_on:
      - express-api